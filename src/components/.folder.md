# Folder: /src/components

1. **地位**: UI 表现层，所有可复用的 React 组件
2. **边界**: 接收 props 数据，输出 JSX 渲染结果
3. **约束**: 禁止直接调用 API，禁止包含业务逻辑

## 成员清单

| 文件 | 角色 | 职责 |
|------|------|------|
| GoalsHeader.tsx | Layout | 顶栏组件，显示4个目标卡片、5条底线和画作背景 |
| TrendsSidebar.tsx | Layout | 左侧趋势栏，显示迷你趋势图 |
| TaskSidebar.tsx | Layout | 右侧任务栏，可拖拽的任务模板列表 |
| WeekCalendar.tsx | Interaction | 周日历组件，拖放目标记录时间块 |

---
⚠️ **触发器**: 一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
